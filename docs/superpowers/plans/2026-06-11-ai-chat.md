# AI 聊天 實作計畫

> executing-plans 逐 task TDD。分支 `feat/ai-chat`。spec：`docs/superpowers/specs/2026-06-11-ai-chat-design.md`。

**Goal:** /chat 變可用 AI 行程助手（工單佇列 + polling + markdown + AI 改完刷新）。
**Tech:** flutter_riverpod 3.x / go_router / dio mock + mocktail / markdown 套件。單一 PR、3 階段。

**核心心智模型**:聊天 state = `List<TripRequest>`(工單,asc),`messages` = `requests.expand(rowToMessages)`。送訊息加樂觀 temp row → POST 換真 row → poll 到 terminal → 更新該 row + completed 時 invalidate trip providers。

---

# 階段 0：models + repository + markdown 依賴

## Task 0.1：TripRequest model

**Files:** 新 `lib/models/trip_request.dart`;test 新 `test/models/trip_request_test.dart`。

- [ ] 測試:fromJson 完整;status parse(completed/failed/open/processing);**unknown → processing**;reply null;isTerminal。
- [ ] 實作:
  ```dart
  /// AI 聊天工單（trip_requests）;一筆 = 一組 user/assistant 對話交換。
  library;

  enum RequestStatus { open, processing, completed, failed }

  RequestStatus parseRequestStatus(String? s) => switch (s) {
        'completed' => RequestStatus.completed,
        'failed' => RequestStatus.failed,
        'open' => RequestStatus.open,
        'processing' => RequestStatus.processing,
        _ => RequestStatus.processing, // unknown 續 poll,不誤判終止
      };

  extension RequestStatusX on RequestStatus {
    bool get isTerminal =>
        this == RequestStatus.completed || this == RequestStatus.failed;
  }

  class TripRequest {
    const TripRequest({
      required this.id, required this.tripId, required this.message, this.reply,
      required this.status, this.submittedBy, this.submittedByDisplayName,
      this.processedBy, this.createdAt, this.updatedAt,
    });
    final int id; final String tripId; final String message; final String? reply;
    final RequestStatus status; final String? submittedBy;
    final String? submittedByDisplayName; final String? processedBy;
    final String? createdAt; final String? updatedAt;

    factory TripRequest.fromJson(Map<String, dynamic> json) => TripRequest(
          id: (json['id'] as num).toInt(),
          tripId: json['tripId'] as String? ?? '',
          message: json['message'] as String? ?? '',
          reply: json['reply'] as String?,
          status: parseRequestStatus(json['status'] as String?),
          submittedBy: json['submittedBy'] as String?,
          submittedByDisplayName: json['submittedByDisplayName'] as String?,
          processedBy: json['processedBy'] as String?,
          createdAt: json['createdAt'] as String?,
          updatedAt: json['updatedAt'] as String?,
        );
  }
  ```
- [ ] 綠 → commit `feat: TripRequest model（AI 聊天工單）`。

## Task 0.2：RequestsRepository + provider + markdown 依賴

**Files:** 新 `lib/api/requests_repository.dart`;`lib/api/providers.dart`(+provider);`pubspec.yaml`(+markdown);test 新 `test/api/requests_repository_test.dart`。

- [ ] 測試:
  - fetchRequests:`GET /requests` query `{tripId, limit:'20', sort:'desc'}` → reply `{items:[row],hasMore:true}` → 解析 items.single + hasMore。
  - fetchRequests cursor:帶 `before`/`beforeId` query。
  - sendRequest:`POST /requests` data `{tripId, message}` → 201 row。
  - fetchRequest:`GET /requests/5` → row。
  - 401 → throwsA ApiError(401)。
- [ ] 實作 `requests_repository.dart`:
  ```dart
  library;
  import '../models/trip_request.dart';
  import 'api_client.dart';

  class RequestsRepository {
    RequestsRepository({required ApiClient client}) : _client = client;
    final ApiClient _client;

    Future<({List<TripRequest> items, bool hasMore})> fetchRequests({
      required String tripId, int limit = 20, String sort = 'desc',
      String? before, int? beforeId,
    }) async {
      final body = await _client.get('/requests', query: {
        'tripId': tripId, 'limit': '$limit', 'sort': sort,
        if (before != null) 'before': before,
        if (beforeId != null) 'beforeId': '$beforeId',
      });
      final map = body as Map<String, dynamic>;
      final items = (map['items'] as List<dynamic>? ?? [])
          .map((e) => TripRequest.fromJson(e as Map<String, dynamic>))
          .toList();
      return (items: items, hasMore: map['hasMore'] == true);
    }

    Future<TripRequest> sendRequest({
      required String tripId, required String message,
    }) async {
      final body = await _client.post('/requests',
          body: {'tripId': tripId, 'message': message});
      return TripRequest.fromJson(body as Map<String, dynamic>);
    }

    Future<TripRequest> fetchRequest(int id) async {
      final body = await _client.get('/requests/$id');
      return TripRequest.fromJson(body as Map<String, dynamic>);
    }
  }
  ```
- [ ] `api/providers.dart`:加 `final requestsRepositoryProvider = Provider<RequestsRepository>((ref) => RequestsRepository(client: ref.watch(apiClientProvider)));`(+import requests_repository、trip_request 若需)。
- [ ] `pubspec.yaml` dependencies:加 `flutter_markdown_plus: ^1.0.0`（或最新）。`flutter pub get`。**若解不到** → 改 `markdown_widget` 或 `gpt_markdown`,記於本 plan 自審。
- [ ] 綠 + `flutter pub get` 成功 → commit `feat: RequestsRepository + markdown 依賴`。

---

# 階段 1：chat_message 投影 + ChatController

## Task 1.1：chat_message.dart（投影 + 前綴/亂碼）

**Files:** 新 `lib/features/chat/chat_message.dart`;test 新 `test/features/chat/chat_message_test.dart`。

- [ ] 測試:rowToMessages — completed→2 氣泡(user + assistant markdown)、failed→assistant isFailed、open/processing→「思考中…」+pendingRequestId、message 空→只 assistant、特殊前綴 `[AI 健檢]`→摘要、亂碼 reply→placeholder。
- [ ] 實作:
  ```dart
  import '../../models/trip_request.dart';

  enum ChatRole { user, assistant }

  class ChatMessage {
    const ChatMessage({
      required this.key, required this.role, required this.text,
      this.isMarkdown = false, this.isFailed = false, this.pendingRequestId,
      this.submittedBy, this.senderName, this.createdAt,
    });
    final String key; final ChatRole role; final String text;
    final bool isMarkdown; final bool isFailed; final int? pendingRequestId;
    final String? submittedBy; final String? senderName; final String? createdAt;
  }

  const Map<String, String> _prefixSummaries = {
    '[AI 健檢]': '已觸發 AI 行程健檢',
    '[行程筆記-lodging-tips]': '已觸發 AI 行程筆記生成（住宿在地建議）',
    '[行程筆記-tips]': '已觸發 AI 行程筆記生成（行前須知）',
    '[行程筆記-emergency]': '已觸發 AI 行程筆記生成（緊急聯絡）',
  };

  const String kGarbledPlaceholder = '訊息含編碼錯誤,無法顯示';
  final RegExp _latin1Run = RegExp('[-ÿ]{3,}');
  final RegExp _c1 = RegExp('[-]');
  bool isGarbledText(String s) =>
      s.contains('�') || _latin1Run.hasMatch(s) || _c1.hasMatch(s);

  String _displayUserText(String message) {
    for (final e in _prefixSummaries.entries) {
      if (message.startsWith(e.key)) return e.value;
    }
    return message;
  }

  List<ChatMessage> rowToMessages(TripRequest r, {required String? myEmail}) {
    final out = <ChatMessage>[];
    if (r.message.isNotEmpty) {
      final t = _displayUserText(r.message);
      out.add(ChatMessage(
        key: '${r.id}-u', role: ChatRole.user,
        text: isGarbledText(t) ? kGarbledPlaceholder : t,
        submittedBy: r.submittedBy, senderName: r.submittedByDisplayName,
        createdAt: r.createdAt,
      ));
    }
    switch (r.status) {
      case RequestStatus.completed:
        final reply = r.reply ?? '';
        final bad = isGarbledText(reply);
        out.add(ChatMessage(key: '${r.id}-a', role: ChatRole.assistant,
            text: bad ? kGarbledPlaceholder : reply, isMarkdown: !bad,
            createdAt: r.updatedAt ?? r.createdAt));
      case RequestStatus.failed:
        out.add(ChatMessage(key: '${r.id}-a', role: ChatRole.assistant,
            text: (r.reply == null || r.reply!.isEmpty) ? 'AI 處理失敗。' : r.reply!,
            isFailed: true, createdAt: r.updatedAt ?? r.createdAt));
      case RequestStatus.open:
      case RequestStatus.processing:
        out.add(ChatMessage(key: '${r.id}-a', role: ChatRole.assistant,
            text: '思考中…', pendingRequestId: r.id, createdAt: r.createdAt));
    }
    return out;
  }
  ```
- [ ] 綠 → commit `feat: chat_message 投影（前綴摘要 + 亂碼防護）`。

## Task 1.2：ChatController（polling 狀態機）

**Files:** 新 `lib/features/chat/chat_controller.dart`;test 新 `test/features/chat/chat_controller_test.dart`。

- [ ] 測試（mock RequestsRepository + override providers）:
  - loadInitial:fetchRequests 回 2 筆(desc)→ requests 反轉成 asc;initialLoading false。
  - send:樂觀加 temp(requests +1)→ sendRequest 回 row(processing)→ temp 換真 + 啟動 poll;poll 回 completed → 該 row 更新 + reply 可見。
  - poll completed → invalidate trip providers(listen tripDaysProvider,送出+poll completed 後它被 invalidate/refetch)。
  - 401(poll)→ authExpired。
  - loadOlder:fetchRequests(before/beforeId)→ prepend。
  - （poll 用 `fake_async` 或把間隔設可注入;簡化:poll 第一次立即抓,測試用立即 completed 的 stub 即可不等 4s — 見實作「立即先抓一次」。）
- [ ] 實作（`NotifierProvider.autoDispose.family`;state 持 `List<TripRequest> requests` asc）:
  - `ChatState{ requests, initialLoading, hasMore, loadingOlder, oldest:(String? createdAt,int id)?, sending, error?, authExpired }` + copyWith（error/oldest 用 sentinel）。
  - `build(tripId)`:`ref.onDispose(()=>_disposed=true)`;排程 `loadInitial()`（`Future(loadInitial)`）;回 `ChatState(initialLoading:true)`。
  - `_repo`=read(requestsRepositoryProvider);`_myEmail`=read(authStateProvider).value?.email;`_myName`=…displayName。
  - `loadInitial`:`fetchRequests(arg, sort:'desc', limit:_pageSize)` → `rows=page.items.reversed`;set requests=rows、hasMore、oldest=(rows.first.createdAt, rows.first.id)（空則 null）;對非 terminal 的 row `_startPoll(id)`。401→authExpired;其他→error。
  - `loadOlder`:guard(hasMore && !loadingOlder && oldest!=null);`fetchRequests(before:oldest.createdAt, beforeId:oldest.id, sort:'desc')` → older=reversed;`requests=[...older,...requests]`;更新 oldest/hasMore;poll 非 terminal。
  - `send(text)`:trim,空/ sending 則 return;`final temp=TripRequest(id:_tempId--, tripId:arg, message:t, status:open, submittedBy:_myEmail, submittedByDisplayName:_myName, createdAt:null)`;requests=[...requests, temp];sending=true。try `row=sendRequest(arg,t)` → `requests=[...withoutTemp, row]`;sending=false;`row.isTerminal? (completed→_invalidate) : _startPoll(row.id)`。on ApiError 403→error '沒有權限';其他→移除 temp + error '送出失敗'。
  - `_startPoll(id)`:`if(_polling.add(id)) unawaited(_poll(id))`。
  - `_poll(id)`:`while(!_disposed){ await Future.delayed(_pollInterval); if(_disposed)break; try row=fetchRequest(id) on ApiError e {if(e.status==401){state=...authExpired;break;} continue;} on Exception {continue;} if(_disposed)break; _replace(row); if(row.isTerminal){ if(row.status==completed)_invalidate(); break; } }` finally `_polling.remove(id)`。
    - **第一次立即抓**:迴圈前先 `try{ final r=await fetchRequest(id); _replace(r); if(r.isTerminal){...; return;} }catch{}` 再進 delay 迴圈（測試可不等間隔即見 completed）。
  - `_replace(row)`:`requests = [for(final r in requests) r.id==row.id ? row : r]`。
  - `_invalidate()`:invalidate tripDetailProvider(arg)/tripDaysProvider(arg)/tripNotesProvider(arg)/tripSegmentsProvider(arg)/favoritesProvider。
  - `_pollInterval=Duration(seconds:4)`;`_pageSize=10`;`_tempId` 從 -1 遞減。
- [ ] 綠 → commit `feat: ChatController（loadInitial/loadOlder/send/polling/完成後 invalidate）`。

---

# 階段 2：ChatScreen + 路由

## Task 2.1：deep-link 映射純函式

**Files:** `lib/features/chat/chat_link.dart`（新）;test `test/features/chat/chat_link_test.dart`。

- [ ] 測試:`mapReplyLink('/trip/abc/notes','abc')`→`/trips/abc/notes`;`/trip/abc`→`/trips/abc`;`/trip/abc/map`→`/trips/abc/map`;`/trip/abc/health`→`/trips/abc`(無對應退時間軸);`https://x`→null(外部,不 in-app 跳)。
- [ ] 實作:`String? mapReplyLink(String href, String tripId)` — 解析 `/trip/<id>/<sub?>`;sub∈{notes,map}→`/trips/<id>/<sub>`;sub 空→`/trips/<id>`;其他 sub→`/trips/<id>`;非 `/trip/` 開頭→null。
- [ ] 綠（併入 2.2 commit）。

## Task 2.2：ChatScreen + 路由接線

**Files:** 新 `lib/features/chat/chat_screen.dart`;`lib/app/router.dart`(/chat → ChatScreen);test 新 `test/features/chat/chat_screen_test.dart`。

- [ ] 測試（mock RequestsRepository + override myTripsProvider/authStateProvider/chat repo）:
  - 行程下拉渲染（`chat-trip-dropdown`,顯示 trip 標題）。
  - 送訊息:輸入 `chat-input` + 點 `chat-send` → 樂觀顯示訊息 + verify sendRequest。
  - completed reply → markdown 文字顯示。
  - 思考中態顯示。
  - my-trips 空 → 「先建立行程」提示。
- [ ] 實作:
  - `ChatScreen`（ConsumerStatefulWidget）:`String? _tripId`;initState 後由 `myTripsProvider` 設預設（第一筆）。
  - AppBar title「AI 助手」;body:
    - my-trips loading/error/data;data 空→提示;否則頂端 `DropdownButton<String>`（key `chat-trip-dropdown`,items=trips displayTitle,onChanged setState _tripId）。
    - `_tripId` 有值 → `_ChatBody(tripId:_tripId!)`（watch `chatControllerProvider(tripId)`）。
  - `_ChatBody`:訊息 `ListView(reverse:true, controller:...)`,items = state.messages 反向;近頂(scroll listener / `ScrollController`)→ `loadOlder`。每 message → `_Bubble`。底部輸入列(`chat-input` TextField + `chat-send`)。`authExpired`→橫幅。
  - `_Bubble`:三方（讀 authStateProvider myEmail）— assistant 左+「Tripline AI」(sage)、自己 accent 右、其他 collaborator 左+senderName+頭像(pink)。assistant 且 isMarkdown → `MarkdownBody(data:text, onTapLink:(t,href,_){ final loc=mapReplyLink(href??'', tripId); if(loc!=null) context.push(loc); })`;否則 `Text`。isFailed→錯誤色;pendingRequestId!=null→「思考中…」+ 小 spinner +（createdAt ≥3 分→「已等候 N 分」）。
  - 送出:`controller.send(text)`,清空 input,捲到底（reverse list → jumpTo 0）。
  - 路由:`/chat` builder → `const ChatScreen()`(import)。
- [ ] 綠 → commit `feat: ChatScreen（行程下拉 + 三方氣泡 + markdown/deep-link + 輸入 + 上捲分頁）`。

---

# 收尾
- [ ] 全 `flutter analyze` 0 + `flutter test` 全綠。
- [ ] finishing：push + PR（base master）。
- [ ]（選配）iOS smoke。

## 自我審查（plan vs spec）
- 工單契約(GET 雙形狀/POST/poll)→ 0.2;狀態機/長等待/invalidate → 1.2;markdown/deep-link/三方/前綴/亂碼 → 1.1+2.x。
- markdown 套件實際採用:____（pub get 後填）。
- ValueKey 一致;polling 第一次立即抓（測試友善）;無 placeholder。
- per-trip + 下拉預設最近;completed 後 invalidate trip 系列 providers（最關鍵點）。
